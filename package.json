{
  "name": "orx",
  "version": "0.1.0",
  "description": "OpenRouter-native terminal coding agent.",
  "type": "module",
  "bin": {
    "orx": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/cli.ts",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "eventsource-parser": "^3.1.0",
    "smol-toml": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
